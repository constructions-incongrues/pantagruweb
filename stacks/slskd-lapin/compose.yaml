services:
  slskd:
    restart: unless-stopped
    image: slskd/slskd:0.23.2
    user: "${UID-1000}:${GID-1000}"
    networks:
      - saltbox
    env_file: seed.env
    labels:
      traefik.enable: true
      com.github.saltbox.saltbox_managed: true
      traefik.http.routers.pantagruweb-slskd-lapin.entrypoints: web,websecure
      traefik.http.routers.pantagruweb-slskd-lapin.middlewares: globalHeaders@file,redirect-to-https@docker,robotHeaders@file,cloudflarewarp@docker
      traefik.http.routers.pantagruweb-slskd-lapin.rule: Host(`lapin-gargamelle.pantagruweb.club`)
      traefik.http.services.pantagruweb-slskd-lapin.loadbalancer.server.port: 5030
    volumes:
      - /mnt/remote/vip_audio/Audiothèque/Mange Lapin/__Gargamelle:/app/downloads
      - ./slskd.yml:/app/slskd.yml
      - /etc/localtime:/etc/localtime:ro

networks:
  saltbox:
    external: true
